# CS2 联调：玩家第一次实测

## 当前版本：assistchat.1（优先于以下历史记录）

玩家确认采用自定义生涯助攻规则：多个非击杀贡献者按累计伤害最高、同伤害先达到者选择，只记一次。`career_damage_then_reached_first_v1` 标识该路径；闪光冲突及缺少贡献数据不套用伤害规则。新增台词和扩展字段见根目录《助攻与场内对话说明.txt》。新程序准备比赛时安装内置DLL并冻结聊天配置，旧比赛不追溯变更。

## 最新补充：assistfix.1 助攻判定（优先于下文历史记录）

现场发现同一控制器先后控制两名选手伤害同一目标，旧 ActorOwnership.Assister
把最终击杀者也当成助攻候选，导致虚假的多候选错误。
现在 ResolveAssist 先排除击杀者/死者，再判断唯一贡献；Bot接管前贡献也通过原角色索引查找。
不以伤害最高、当前控制身体或阵容顺序猜助攻，不放宽真实多贡献者的歧义校验。
已完成156项纯C#检查和插件编译，未完成本修复的真实CS2新对局验收。
旧 trace 的 death 条目没有 assister/assistedflash，所以不得把现场回放宣称为完整助攻恢复。
新 trace 增加 assist 条目，包含 controller/killer/victim/flash/resolved/ambiguous/reason/candidates。
对局原始文件只备份在本机私有 .qa 目录，不加入源码包、Release或GitHub。

回归入口：tools/identity-tests/AssistRegression.cs；修复入口：
vendor/CareerMatch/ActorOwnership.cs 与 CareerMatch.Events.cs。

程序：`release/CS2Career-CS2Test/CS2Career-CS2Test.exe`。
这是允许连接真实CS2的桌面联调版，不是之前禁用游戏的IdentityFix/CareerFlow版。
使用这个EXE旁边的 `save/` 和 `extensions/`；不读取其他测试版生涯，不替你推进赛季。
本机路径已预填，沿用Low、混合瞄准、正常道具和真人身份。没有启用游戏内换肤或填写SteamID。

## 第一次怎么试

1. 完全关闭CS2，然后打开上述EXE。创建生涯；想尽快上场可接管职业选手。
2. 到“游戏设置”核对三个路径。你本机已有增强运行组件，本次也已更新CareerMatch，
   不需要重复点击整包安装。另一台机器必须先安装人机增强。
3. 正式比赛测试：接受邀请并推进到自己的比赛，在战报页点“自己打”或“进入第1图”。
   不要点“跳过，按角色数值结算”，那是模拟比赛。
   想先快速看昵称/头像，可以先到“训练赛”测试一张Mirage；训练赛不替代正式比赛的生涯结算验收。
4. 由程序启动CS2（带 `-insecure -condebug`）。按启动提示在CS2选择“与机器人游戏 → 竞技”及指定地图，
   加入程序提示的CT/T方；不要去官方匹配或联网服务器。
5. 观察聊天中的难度、`9/9`和哈希短码；按Tab应看到真人加9名Bot，昵称为选手名，
   头像为队标或中性默认图，不应是随机Steam头像。
6. 打完一张正式地图，保留桌面程序运行并切回来。核对十人K/D/A、ADR、KAST、Rating。
   当前实战导出尚未包含完整逐回合事件记录，不能把模拟比赛的回合展示当成实战已验收。
   不完整时不得以0补人；记录错误提示，使用“录入战绩”重试，不要立即重开覆盖现场。
7. BO3的下一张图或更换难度前，必须完全退出CS2，再回到生涯启动下一图。
   第一轮先保持Low及关闭换肤；基础链路确认后，再分别试Medium/High和可选换肤。

## 出问题时保留什么

- 给我游戏聊天、Tab计分板、桌面战报/错误提示的截图，并说明地图、阵营、是否换边后才异常。
- 暂时不要再开启另一场；游戏目录中的 `addons/counterstrikesharp/plugins/CareerMatch/`
  含 `match_request.json`、`match_result.json` 和 `match_result.best.json`，下一场会写入新数据。
- 游戏 `console.log` 是 `-condebug` 的诊断日志。无需把整个Steam目录或个人库存发出来。
- 插件备份为同目录 `.career-backup` 后缀。本轮只部署CareerMatch.dll和deps.json，
  没有批量更新第三方插件DLL，也没有下载替换换肤签名。

## 已验证与未验证

### 2026-09-10：开赛等待状态 .4

十人绑定短暂未就绪不再污染StatisticsError；无计分遗漏时可恢复，确有遗漏继续拒绝录入。
初始化/设置完成前的round_start不会作为正式比赛处理。接管成功不再往游戏聊天发提示，
因此后续应查看后台takeover_events/identity_traces，不以没有聊天提示判定接管失败。
新增10项状态机回归；原生CS2新对局仍待验证。旧失败结果不自动清错或补录。

### 2026-09-10：换边比分与非计分事件误拦截 .3

FURIA/FUT实测14次接管全部成功，上一版拒绝原因未细分，不能从旧日志反推唯一失败事件。
修复当前CT/T比分与开局队名混用；比分类别从当前十人阵营映射，不靠半场公式。
已知误判路径（死亡后道具伤害、Bot接管前投掷、非计分闪光通知）加入修复和回归。
新日志包含identity_rejected、grenade_thrown、score_side_mapping；
回传identity_resolver_version=controller_link_v3，score_identity_policy=opening_roster_side。
本节取代下方关于Pawn初始归属的优先级描述，以直接控制器关系为准。
尚未完成本版CS2完整实测，不把控制快照回放当作十人全量战绩已验收。

### 2026-09-10：接管事件适配热修复 .2

上一候选版在真实对局中失败：结果含接管校验错误，takeover_events 为空，
后续击杀仍可能落在真人行。DLL 已是新版，并非用户没有更新。
上游 CounterStrikeSharp #1102 记录了 bot_takeover 两个字段均指向真人的情况。
本版改用回合初始身体归属与控制器交换关系确认；未确认时暂停记账，不回退给真人。
难度和买枪参数不变。原生 TAB 仍由 CS2 管理，本补丁处理生涯回传账本。

新增16项事件适配回放检查（合计46项纯C#检查）及141项隔离Python检查通过。
这是自动化证据，不宣称本版已经实际完成CS2对局。
接管成功应提示“接管某选手，战绩记入该选手”；结束后检查该Bot和真人两行，
并保留插件 identity_traces 中的对应nonce日志。新结果标记 round_pawn_control_v2。
本节取代下面上一候选版的接管完成描述；旧对局不依据猜测重新分配。

### 2026-09-10：原参数、买枪和接管修复候选版

本节取代下方9月9日的参数方案：High 恢复固定来源的特殊加速度原文，
Medium 按生涯档位加载匿名微调。突破改用 RiflePro＋RusherPersonality。
CareerMatch 事件身份按被控制身体的原选手绑定，控制者昵称/头像映射保持独立。
BotBuy 生涯补丁增加延迟实体检查，并保留 SSG08 不做随机 SMG 替换。
新回传的 takeover_events 记录接管回合、控制者和被接管选手。

141项隔离 Python 测试、10组前端测试及30项纯C#身份/归属/哈希检查通过。
实际候选EXE已通过隔离启动导入、内置资源检查和三档9人VPK生成检查，
不是实际窗口操作或CS2实战测试。完整说明见 RELEASE_NOTES.md。
更新只替换 CareerMatch／BotBuy；不更新历史换肤DLL，不覆盖上一场请求和战绩。
请完全退出CS2，通过更新程序生成新比赛后验证；不要直接复用旧VPK判断新调校效果。

### 2026-09-09：基础难度与生涯档位分离

- 最新约定替代旧的 Low −12／Medium 0／High +6 分偏移；不改变模拟赛胜率或生涯 Rating。
- Low／Medium／High 只选择原 Bot Improver 对应 Default 的基础调校。
  个人档位统一复用 Medium 的匿名 Rank／Pro 模板，以免原包少量不同的 Rank 参数混进难度层。
- 当前位置能力 + 0.5 × 状态（限制45—100）决定个人档位：
  65以下按位置选 Rank；65—74 ProSlow；75—84 ProSteady；
  85—89 按属性选 ProFast／ProPrecise；90以上 ProTop。
  同一个选手切换难度，评分和个人档位不变。成长、衰退或状态跨档才更换模板。
- 个人档案末尾显式写入最终数值，六个 LookAngle 参数始终取所选难度的 Default；
  反应与精度参数取个人档位，武器偏好与性格按位置组合。
  不复制 Medium 中针对具体选手的额外瞄准覆盖，也不另设95／98分明星加成。
  因此同一档内相邻评分目前可以使用相同参数，不冒充逐分连续增强。
- 资源位于 `cs2career/data/botprofile_presets/`。三个 DB 保留清理后的原始模板提取，
  `presets.json` 记录原 VPK 来源哈希；`cs2career/cs2/improver_presets.py` 负责两层组合，
  `profiles.py` 负责按当场数据选档、生成9人并校验。
- 原 High 的两处加速度是多小数点非法值，替换为20000，其余基础值保留。
  这是安全兼容调校，不保证与原版极端数值的实际手感完全一致。
- 新清单记录 `difficulty_model=bot_improver_base_career_tier_v1`、原预设哈希和
  组合模板哈希，Python 与 CareerMatch 校验同一摘要。没有模型标记的待赛旧档案仍明确显示旧版调校。
- 完全退出 CS2 后，通过程序生成新比赛再测试；聊天应显示“人机增强基础预设 + 生涯能力档位”、
  当前难度和9/9。不修改已经保存的战绩、库存或上一场回传文件。
- 隔离测试检查分层不互相覆盖、选档边界、状态跨档、难度重建时属性保留、
  9人 VPK 内容、有限参数及跨语言清单哈希。新难度尚未完成实际 CS2 对局验收；
  接管 Bot 的战绩归属问题也没有在本次难度调整中修复。

### 2026-09-09：昵称维护与统计错误解耦

- 新发现：`OnPlayerDeath` 将重复死亡写入 `_contractError` 后，`ApplyBotNames`
  和身份维护直接退出，导致 BotHider 后续恢复内部档案名时无法纠正。
  现场安装哈希与上一轮修复一致，不是玩家启动了旧插件。
- 新增 `SessionHealth.cs`，分别保存资源/身份契约错误与统计错误。
  统计异常仍禁止录入，但不停止可信请求的昵称、头像维护；无效契约仍禁止改名。
  每秒仅检查并纠正变动昵称，原有较低频率维护继续同步头像及 BotHider persona。
- 饰品图片修复自动 Grid 行的最小尺寸溢出：限定 `minmax(0,1fr)`、裁剪图片容器，
  图片及其透明区域禁用鼠标命中，不再覆盖购买/装备按钮。
- 自动验证：130 项隔离 Python 测试、8 项身份检查、6 项会话状态检查及饰品脚本通过。
  图片包含范围目前有 CSS 源码回归检查；修复后 EXE 点击区域仍需实窗复测。
- 已重新打包原路径 EXE，安装的 CareerMatch DLL SHA-256 为
  `a62335ece3d6526fc31133656bb2ec21b713550a4641a6514a564995bb232ba5`，
  更新前 DLL 备份后缀为 `.before-names-fix-20260909`。
- **未完成**：真人接管 Bot 时应按被接管选手归属事件的逻辑，以及实际 BotProfile
  内存参数回读。此轮修复不能当成这两项已完成，也不解除已有异常战绩的拦截。

### 2026-09-09：实战身份错绑修复

- 用户首场 Ancient 的原始回传出现 KSCERATO 26 杀、29 死，而比分 9:13 仅 22 回合。
  原始请求和两份结果已留在本地 `.qa/reports/cs2-identity-issue-20260909/`，不打进发布包。
  该结果不能凭记忆还原；本次不修改已经保存的旧战绩，也不推算真人应有的击杀数。
- CareerMatch 不再把所有 `!IsBot` 槽位覆盖到真人 ID：独立的 `IdentityBindings.cs`
  按连接保留绑定，Bot 使用请求档案名/合成 SteamID/唯一显示名，真人使用经过认证的 Steam 身份。
  同槽位新连接会清理旧绑定；重复身份、未知 Bot 和多真人歧义不再按阵营顺序补位。
- 暖身必须确认唯一十人及 5 对 5 阵容才继续计时；身份未齐会在聊天说明原因。
  昵称、头像及真人选边均按绑定处理，不依赖 BotHider 可改变的展示标志。
- 新回传增加 `identity_bindings`（槽位到生涯 ID，不导出真人 Steam 账号）。
  C# 和 Python 均阻止超回合死亡及死亡/存活不守恒；Python 有 ID 时禁止昵称兜底。
  重复死亡事件拒绝统计；换边后的补枪继续使用开局身份阵营。13:12 不再视为正常终场。
- 饰品详情显示个人余额、价格、购买后余额或具体差额，支持刷新；失败不会直接关闭弹窗。
  不将俱乐部资金改为个人资金。所有目录饰品的足额购买后端测试通过；用户报告的
  “有钱但部分枪皮肤无法点击”尚未在真实窗口复现，不能断言仅是余额不足。
- 本轮自动验证：130 项 Python 测试、10 个 Node 测试脚本、8 项 C# 纯身份逻辑检查通过。
  Python 统一使用 `tools/run_tests.py`，不读取或推进玩家生涯。
  C# 检查项目是 `tools/identity-tests/IdentityTests.csproj`；本机缺少独立 .NET 10 运行时，
  用已安装的 .NET 11 preview 执行编译后的测试 DLL；游戏仍由自身 CSS 运行时加载插件。
- 已重新打包同一路径的 CS2Career-CS2Test.exe，并将两个 CareerMatch 文件安装到 CS2。
  DLL SHA-256：`780dfa75fa4e5bc838c8137ace80e075623276c0198c91241200467b74766287`。
  更新前插件保留为 `.before-identity-fix-20260909` / `.identity-replaced-20260909`。
  现有 save、配置、库存未改动。修复后真人实战和购买窗口仍待玩家复测。

已验证：CareerMatch从当前源码编译；本机CSS运行配置是net10.0；两份插件部署后SHA-256核对；
临时目录内三档完整准备链路；复制失败/CS2正在运行时阻止部署；桌面轮询只自动提交完整终场，
不会仅凭13分结束；123项Python测试和9组前端检查通过。
编译使用已安装的预览SDK，目标仍为net10.0；离线NuGet漏洞审计警告不代表审计通过。

未验证：成品EXE原生窗口实际操作、CS2内插件加载是否兼容当前游戏版本、实际头像/改名、
换边/重连等实战统计及最终真实生涯结算。以上需要本次玩家实测，不能用打包成功代替。
